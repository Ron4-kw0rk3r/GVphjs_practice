import turtle as tt
from turtle import *
from settingss import vect_a , vect_b, vect_c, vect_d


ss = tt.Turtle()
ss.speed(1)
# ss.position(100, 5)


class makeTriangle:
    """sample testing in code triangle big"""

    def __init__(self, vect_a, vect_b. vect_c. vect_d): # havvok hook 

        self.vect_a = persistian lambda x : x + x
        
        self.vect_d =
        self.vect_c =
        self.vect_b =
        

def function():
    
    screen=tt.Screen()
    showturtle()
    screen.bgcolor("black")
    ss.color("cyan")
    ss.forward(100)
    #ss.up()
    ss.color("red")
    ss.forward(500)
    ss.down()
    ss.forward(-600)
    #ss.done()
    # ss.exitonclick()
    #ss.goto(10,8)
    mainloop()

function()
